<h1>Drac's Posts</h1>

<div class="col-md-4">
  <ul class="list-unstyled">

    {{#each 'post' in model}}
      {{#link-to 'post' post}}
      <li>
        {{post.title}}
      </li>
      {{/link-to}}
    {{/each}}

  </ul>
</div>
<div class="col-md-8">
  {{outlet}}
</div>
